# CLAUDE.md — Amine Academy — سجل المشروع

> هذا الملف مرجع دائم لكل ما تم بناؤه وإصلاحه في المشروع.
> يُحدَّث بعد كل جلسة عمل كبيرة.

---

## 1. نظرة عامة على المشروع

**Amine Academy** — منصة علاج وتعليم متكاملة للأطفال ذوي الاحتياجات الخاصة (توحد، عسر قراءة، اضطراب تركيز، إلخ).

- **المستخدمون:** أخصائيون، أولياء أمور، أطفال
- **نوعا العائلة:** `online` (تسجّل بنفسها وتدفع) و`in-person` (طفل يُتابَع حضورياً، يُنشئ له الأخصائي حساباً بلا باقة ولا دفع) — الفارق في `Parent.accountType`، ويُقرأ عبر `lib/account-type.ts`
- **اللغة الرئيسية:** العربية (RTL)، مع واجهة ثلاثية AR/EN/FR
- **البيئة:** Next.js 15 App Router + React 19، TypeScript، Tailwind CSS، Redis (upstash)، Resend (إيميل)، Vitest
- **المستودع:** `aminehamdipro25-crypto/amine-academy`
- **الفرع الرئيسي:** `main` (كل الكود يُدفع مباشرة إليه)

---

## 2. هيكل المشروع

```
app/
├── page.tsx                    # Landing page (الصفحة الرئيسية)
├── dashboard/
│   ├── (admin)/                # لوحة تحكم الأخصائي
│   │   ├── clients/            # إدارة العملاء
│   │   ├── appointments/       # المواعيد
│   │   ├── messages/           # الرسائل
│   │   ├── payments/           # المدفوعات
│   │   ├── programs/           # البرامج الأسبوعية
│   │   ├── reports/            # التقارير السريرية
│   │   ├── analytics/          # التحليلات
│   │   ├── exercises/          # مكتبة التمارين
│   │   ├── learning-difficulties/
│   │   ├── settings/           # الإعدادات والأسعار
│   │   ├── specialist-toolkit/ # حقيبة المختص (التقييم)
│   │   ├── apa-planner/        # دليل حصص النشاط البدني المعدّل
│   │   ├── treatment-plan/     # خطة العلاج
│   │   ├── stories/            # مكتبة القصص
│   │   ├── monitor/            # مراقبة الأخطاء
│   │   ├── audit/              # سجل المراجعة
│   │   └── staff/              # إدارة الموظفين
│   └── login/
├── parent/                     # بوابة أولياء الأمور
│   ├── (portal)/               # dashboard الوالدين
│   ├── login/
│   ├── forgot-password/
│   └── reset-password/
├── session/[id]/               # جلسة العلاج التفاعلية
├── register/                   # تسجيل أولياء الأمور
├── checkout/                   # الدفع
├── payment/                    # تأكيد الدفع
├── demo/                       # جولة تجريبية
├── blog/                       # المدونة
├── legal/                      # الصفحات القانونية
└── api/                        # جميع API routes

components/
├── session/exercises/          # ~80 تمرين تفاعلي (على الشاشة)
├── dashboard/                  # مكونات اللوحة (ApaSessionPlanner، AddInPersonClientForm، …)
├── parent/                     # مكونات بوابة الولي (SpecialistAssessments، …)
├── session/                    # مكونات الجلسة
└── ...

lib/                            # مساعدات، أنواع، ثوابت
```

### مساعدات `lib/` — مصدر واحد لكل مفهوم

> كل واحد منها وُلد من عيب حقيقي. **لا تكتب بديلاً محلياً لأيٍّ منها** — التكرار هو ما أنتج العيب أول مرة.

| الملف (`lib/`) | المفهوم | حارسه (`tests/`) |
|---|---|---|
| `format.ts` | التواريخ والأوقات والأرقام ورابط واتساب | `format.test.ts` + `numerals.test.ts` |
| `week.ts` | أسابيع تبدأ الاثنين بتوقيت UTC — الأسبوع الغائب = صفر لا أسبوع أقدم | `week.test.ts` |
| `age.ts` | عمر تقويمي دقيق + الفئة العمرية، ويرفض تواريخ لا وجود لها | `age.test.ts` |
| `base-url.ts` | أصل الروابط داخل البريد (مع احتياط `VERCEL_URL`) | — |
| `person-name.ts` | تعقيم أسماء الأشخاص قبل التخزين | `person-name.test.ts` |
| `safe-storage.ts` | كل وصول للتخزين المحلي — لا يرمي أبداً | `safe-storage.test.ts` |
| `chat-input.ts` | حدّ حجم وعدد رسائل الروبوت العام | `chat-input.test.ts` |
| `account-type.ts` | حضوري مقابل أونلاين — الغياب = `online` | `account-type.test.ts` |
| `seeded-random.ts` | بذرة مشتركة فيتطابق محتوى التمرين على الشاشتين | `seeded-random.test.ts` |
| `apa-record.ts` / `apa-trend.ts` | تعقيم وتجميع حصص APA | `apa-record.test.ts` |
| `google-oauth.ts` | دخول الولي بحساب Google — PKCE و`state` و`email_verified`، ولا يُنشئ حساباً | `google-oauth.test.ts` |
| `assessment-compare.ts` | مقارنة مقياس بآخر مرة أُجري فيها — الانخفاض تحسّن **إلا** في مقاييس القدرة (`ABILITY_SCALES`) | `assessment-compare.test.ts` |
| `cognitive-tasks.ts` / `cognitive-profile.ts` | بطارية الأداء المعرفي وملفها — قياس ملاحَظ، بلا درجة كلية ولا مئين ولا شدّة | `cognitive-profile.test.ts` |

---

## 3. ما تم بناؤه — بالتفصيل

### 3.1 الصفحة الرئيسية (Landing Page)

- **بُني من الصفر** بتصميم متدرج فاتح (light gradient)
- أقسام: Hero، HowItWorks، Features، Pricing (QAR + TND)، Blog، Legal
- دعم ثنائي اللغة AR/EN لجميع الأقسام
- صفحة Demo Tour تفاعلية
- Logo مخصص (SVG دماغ)

### 3.2 منظومة المصادقة (Auth)

| الميزة | الحالة |
|---|---|
| تسجيل أولياء الأمور + تفعيل بالإيميل | ✅ |
| كود تفعيل الطالب (student access code) | ✅ |
| تسجيل دخول الأخصائي (Redis + HMAC session) | ✅ |
| نسيان كلمة المرور + إعادة التعيين | ✅ |
| إعادة تعيين كلمة المرور من لوحة الأخصائي | ✅ |
| انتهاء صلاحية الجلسة (timeout) | ✅ |
| تصلب ضد Brute Force (rate limiting) | ✅ |
| حماية جميع endpoints بالمصادقة | ✅ |

### 3.3 لوحة تحكم الأخصائي (Admin Dashboard)

جميع الصفحات أُعيد تصميمها بالكامل بنظام تصميم موحد (light theme + motion):

- **Clients** — قائمة العملاء، تفاصيل كل عميل، تعليق/حذف، انتحال هوية
- **Appointments** — إنشاء، تعديل، إلغاء مواعيد
- **Messages** — مراسلة أولياء الأمور + auto-refresh
- **Payments** — متابعة المدفوعات، حالة كل عميل
- **Programs** — بناء برنامج أسبوعي يدوياً أو بالذكاء الاصطناعي
- **Reports** — تقارير سريرية تفصيلية (ذكاء اصطناعي + بيانات حقيقية)
- **Analytics** — إحصاءات عامة (بدون PII)
- **Exercises** — مكتبة التمارين
- **Learning Difficulties** — اضطرابات التعلم
- **Settings** — الأسعار، الخصومات، تشخيص الإيميل، اختبار البريد
- **Specialist Toolkit** — أدوات سريرية إضافية
- **Staff** — إدارة الموظفين

### 3.4 منظومة الإيميل (Resend)

| الإيميل | متى يُرسل |
|---|---|
| تفعيل الحساب | عند التسجيل |
| إعادة تعيين كلمة المرور | عند الطلب |
| إشعار للأخصائي عند رسالة وارد | عند كل رسالة من ولي الأمر |
| إشعار لولي الأمر عند رد الأخصائي | عند كل رد |
| تأكيد الحجز للأخصائي | عند حجز موعد |
| تذكير 24 ساعة قبل الموعد | cron يومي |
| تذكير ~1 ساعة قبل الموعد | cron يومي |

### 3.5 منظومة الدفع

- اكتشاف العملة الجغرافي (QAR/TND)
- Checkout Page → FAWRAN / تحويل بنكي
- لوحة متابعة الدفع في الأدمن
- بيانات بنكية حقيقية

### 3.6 بوابة أولياء الأمور

- لوحة متابعة تقدم الطفل
- مراسلة الأخصائي
- عرض ملخص الجلسات (بدون تفاصيل سريرية حساسة)
- عرض البرامج والتقارير

### 3.7 جلسة العلاج التفاعلية — `/session/[id]`

أكثر جزء تطور في المشروع:

#### مكونات الجلسة المستخرجة:
- `SessionHeader` — الرأس مع معلومات الطالب
- `StudentTimerDisplay` — عداد الوقت
- `SessionToolbar` — شريط الأدوات (بطاقات، موقت، موسيقى)
- `SessionPhaseBar` — شريط تقدم المراحل
- 6 panels/modals مستخرجة

#### ميزات الجلسة:
- **مراحل قابلة للتعديل** — الأخصائي يحدد مدة كل مرحلة قبل البدء
- **محرك صعوبة تكيفي** — يرفع/يخفض صعوبة التمرين تلقائياً بناءً على الأداء
- **ذكاء جلسة حي** — توصيات في الوقت الفعلي للأخصائي
- **إخفاء Chrome تلقائي** — يختفي الرأس/الشريط عند بدء التمرين
- **زر إعادة التشغيل** — يعيد التمرين الحالي من البداية
- **تبديل حجم الشاشة** — صغير/كبير
- **لوح الرسم** مع إخفاء تلقائي عند التمرين
- **خريطة التقدم** (Duolingo-style) — نجوم تضيء بعد كل تمرين
- **احتفال نهاية الجلسة** مع الخريطة
- **حفظ تلقائي** مع flash "محفوظ"
- **تقرير نهاية الجلسة** سريري متكامل

### 3.8 التمارين التفاعلية — ~80 تمرين على الشاشة + 45 تمرين بدني في المكتبة

#### التمارين الرئيسية (مجموعات):
- **التركيز والانتباه:** TapTarget، SustainedAttention، FlashCount، NumberSearch، NBackTask، TargetTracking
- **الذاكرة:** MemoryCards، SequenceMemory، AuditoryMemory، AudioSequenceRepeat
- **اللغة والقراءة:** ReadingCards، SpellingBee، WordBuilder، LetterMatch، WordRecall، ListeningComprehension
- **الرياضيات:** MathFlash
- **التفكير المنطقي:** LogicSort، AnalogiesGame، PatternPuzzle، PatternMatch، IfThen
- **المشاعر والاجتماعي:** EmotionCards، EmotionMirror، EmotionVolume، MoodMeter، SocialProblemSolving، ConversationStarter، SensoryCheckIn
- **التوحد:** ChoiceBoard، FirstThenBoard، BehaviorContract، DirectionFollow، VisualSchedule، VisualMatch، ImitationMirror، SocialScenarios، CategorySort
- **الاسترخاء:** BreathingGuide، BalloonControl، CalmCorner، BodyScan، PhysicalExercise
- **اللعب:** SimonSays، ColorGrid، StroopTest، FingerGym، SequenceTap، FingerGym، SequenceTap
- **متفرقة:** VerbalFluency، PictureWordCards، WordBuilder، VisualMatch

#### قواعد البيانات الموسّعة:
- LogicSort: 12 سؤال
- AnalogiesGame: 24 قياس
- SocialScenarios: سيناريوهات مفصّلة
- SpellingBee: 20 كلمة
- EmotionMirror: بنك موسّع
- StroopTest: ألوان موسّعة
- WordRecall: قوائم موسّعة
- CategorySort: فئات موسّعة

---

## 3.9 المزامنة اللحظية بين الأخصائي والطفل (المحور الأساسي — جلسة 13)

> جوهر المنصة: الأخصائي يُدير الجلسة على `/session/[id]` والطفل يدخل من بوابة الوالدين على `/session/[id]/kid`، ويتفاعلان مع **نفس** المحتوى لحظياً مع فيديو متبادل.

### البنية: "إيقاظ + Redis مصدر الحقيقة"
- **`lib/realtime-server.ts`** — نشر أحداث Pusher (server). كل تغيير يكتب في Redis (المصدر الدائم) ثم يُرسل نبضة إيقاظ صغيرة فقط؛ المستقبِل يُعيد جلب البيانات من الـ API المُصادَق. `SessionEvent`: `live | content | whiteboard | timer | noise | card | kid-status | presence | readiness | progress | reaction`.
- **`lib/realtime-client.ts`** — اشتراك Pusher (client)، اتصال واحد مُشترك لكل تبويب (ref-counted). `realtimeEnabled()`، `subscribeSession()`، `subscribeConnectionState()`.
- **التدهور الرشيق:** إن غابت مفاتيح Pusher، النشر no-op والعملاء يعملون بالتحديث الدوري (poll). المنصة تعمل بدون Pusher؛ إضافته ترفع اللحظية من ~1s إلى ~100ms بلا تغيير كود.
- **مفاتيح Pusher الستة في Vercel** (`NEXT_PUBLIC_*` تُبنى وقت البناء → تحتاج إعادة نشر). مؤشر عائم عند الأخصائي يُظهر «مباشر / تحديث دوري».

### الفيديو — Daily.co Prebuilt Iframe
- `components/session/DailyVideoCall.tsx` — انتقل من call-object إلى `DailyIframe.createFrame` (prebuilt) — أنهى نهائياً خطأ CSP لتحميل bundle من `*.dailywebrtc.net`. الكاميرا تعمل عبر `Permissions-Policy: camera=*` على `/session/*` + `allow=` على iframe.
- `lib/daily.ts`: `dailyRoomNameFor()` = `amine-${sha256(id).slice(0,24)}` — فكّ ربط طول اسم الغرفة عن طول الـ appointmentId (حدّ 41 حرف).
- `DraggableVideoPiP` — صندوق قابل للسحب؛ يبقى الاتصال حياً عند التصغير (لا يُهدم)، ويُحرّر `right/bottom` عند السحب حتى يتحرك فعلاً.

### تطابق المحتوى — بذرة عشوائية مشتركة
- **`lib/seeded-random.ts`** — PRNG حتمي (mulberry32): `createRng`, `shuffleWithRng`, `pickWithRng`, `randIntWithRng`, `randBoolWithRng`.
- **المشكلة الجذرية:** كل تمرين يُحمَّل كشجرتين React مستقلتين؛ أي `Math.random()` يُنتج محتوى مختلفاً على كل شاشة. **الحل:** الأخصائي يُنشئ `seed` واحداً لكل تمرين (يُحسب أثناء الرسم عبر ref محروس — لا useEffect، حتى يكون صحيحاً من أول render)، يُرسل عبر قناة `live`. حُوّلت **كل التمارين الـ63** لاستخدام البذرة → نفس البطاقات/الأسئلة/المواضع تماماً على الشاشتين.

### رؤية إجابات الطفل لحظياً (Live per-answer feedback)
- **`ExerciseProgressUpdate`** (`lib/types.ts`): `{ answered, total, correct, errors, lastCorrect? }`.
- مسار `/api/sessions/[id]/progress` (POST من الطفل مُخفَّف، GET للأخصائي، حدث `progress`، TTL قصير).
- **60 تمريناً** تستدعي `onProgress` عند كل إجابة (قيم مُحسبة مسبقاً nc/ne؛ tap-games مستمرة تستخدم refs و`total:0`). الـ19 المتبقية أدوات استرخاء/تقييم ذاتي (لا صح/خطأ).
- **لوحة عائمة عند الأخصائي** (مستقلة عن الرأس المُخفى): شريط تقدم + ✓/✗ + وميض آخر إجابة.

### أدوات تحكم الأخصائي — انفجارات التشجيع
- مسار `/api/sessions/[id]/reaction` (POST للأخصائي فقط، `{type, id}` بـ nonce، TTL 60s، حدث `reaction`).
- شريط عائم (⭐🎉👏❤️👍🌈) يُطلق احتفال ملء الشاشة على جهاز الطفل (`KidReactionBurst`).

### تجربة الطفل
- **`lib/feedback-sound.ts`** — نغمات Web Audio موحّدة عبر كل التمارين (تُشغَّل من `onProgress` بلا لمس أي تمرين): نغمة صاعدة للصحيح، هادئة للخطأ (ليست جرس حاد — مهم لذوي الاحتياجات) + وميض حواف أخضر/أحمر (`KidAnswerGlow`).
- **`KidProgressMap`** — خارطة تقدم (Duolingo-style) تظهر بين التمارين: عقدة لكل تمرين مُنجز بنجومه (1-3 حسب النتيجة) + مجموع النجوم + عقدة «التالي» تنبض. محفوظة في localStorage (`kid-stars:{id}`) فتبقى بعد إعادة التحميل. تحل محل شاشة الانتظار الساكنة.
- **`KidReadinessScreen`** — الطفل يجيب تقييم الجاهزية بنفسه (وجوه/رموز)، يُزامَن حياً للأخصائي.

### الحضور والموثوقية
- `presence` — نبض ثنائي المفتاح (`session:presence:{specialist|kid}:{id}`، TTL 15s) → مؤشر «غادر الطفل» صادق.
- **إعادة محاولة رابط الفيديو** (kid) حتى ينجح (backoff مُقيَّد بـ10s) — فشل واحد كان يترك الطفل بلا فيديو كامل الجلسة.
- **إعادة مزامنة عند عودة الاتصال** (الطرفان) — عند إعادة اتصال Pusher تُعاد مزامنة كل القنوات فوراً بدل انتظار الـ poll.

---

## 3.10 حلقة الطفل الحضوري — من التقييم إلى بوابة الولي (جلسات 17-20، أُغلقت في 33-34)

> المسار الذي يستخدمه الأخصائي فعلياً مع طفل يُتابعه وجهاً لوجه. كل حلقة فيه تُغذّي التي بعدها.

```
حقيبة المختص  →  حساب حضوري  →  تقييم محفوظ  →  حصص APA  →  تقرير الولي  →  بوابة العائلة
```

### الخطوات كما تجري على الشاشة
1. `/dashboard/specialist-toolkit` — اسم الطفل + **تاريخ الميلاد** (العمر يُحسب تلقائياً ويصير للقراءة فقط).
2. تحذير أصفر: «هذا التقييم غير مرتبط بطفل — لن يُحفَظ». كل ما يُقاس قبل الربط يبقى في الذاكرة فقط.
3. زر **«إنشاء حساب حضوري لهذا الطفل»** → نموذج مُعبّأ مسبقاً (الاسم، تاريخ الميلاد، مجال الاهتمام المستنتج من `concerns`).
4. عند الحفظ يُربط الطفل فوراً (`onLinked` → `linkChild`) → التقييم يُحفَظ تلقائياً، وتظهر «التقييمات السابقة»، ويمكن حفظ البرنامج المولَّد بالـAI.
5. `/dashboard/apa-planner` — تسجيل حصص النشاط البدني بمؤشرات تلك الفئة العمرية.
6. `/dashboard/reports/new` — التقرير يحمل التقييم + ملخّص APA (يُحتسب في الخادم).
7. `/dashboard/clients/[id]` — استخراج رابط الدخول وتسليمه للعائلة.
8. `/parent/reports` — **العائلة ترى التقييم نفسه** (قسم «تقييمات الأخصائي»). كانت هذه الحلقة مقطوعة حتى جلسة 33: التقييم يُحفَظ ولا سطح عند الولي يقرؤه.

### الملفات المحورية
| الملف | الدور |
|---|---|
| `lib/account-type.ts` | `isInPersonAccount` / `isPayingAccount` — الغياب = `online` |
| `lib/age.ts` | حساب عمر تقويمي دقيق + الفئة العمرية (مصدر واحد للثلاثة) |
| `lib/apa-plan-data.ts` | خطط APA المؤلَّفة (ADHD/ASD × ٤ فئات) |
| `lib/apa-exercise-link.ts` | ربط كل مرحلة بتمارين مكتبة المنصة الـ٤٥ |
| `lib/apa-record.ts` + `lib/apa-trend.ts` | تعقيم وتجميع حصص APA؛ `indicatorTrend` منفصل ليخفّ حزم العميل |
| `POST /api/admin/clients` | إنشاء ولي + طفل حضورياً في خطوة واحدة |
| `PATCH /api/admin/students/[id]` | تصحيح التشخيص/الشدّة **بعد** التقييم + **ملء** تاريخ ميلاد غائب (لا تغيير موجود) |
| `POST/GET /api/admin/apa-sessions` | تسجيل الحصص وقراءة ملخّص الفترة |
| `GET /api/parent/assessment` | ما تراه العائلة — بلا `answers` ولا `assessedByActorId` ولا `clinicalNotes` |
| `components/parent/SpecialistAssessments.tsx` | عرض التقييم في بوابة الولي، ويُميّز من أجراه |

### مبادئ نزاهة مطبَّقة في هذه الحلقة
- **«مجال الاهتمام المبدئي» لا «التشخيص»** — يُسجَّل عند الإدخال قبل أي مقياس، فافتراضه «غير محدد بعد»، ومكتوب تحته أنه ليس تشخيصاً، ويُصحَّح لاحقاً من ملف العميل.
- **ملخّص APA يُعاد حسابه في الخادم** — أي `apa` في جسم الطلب يُتجاهَل.
- **حصة واحدة = «لا يمكن استنتاج اتجاه بعد»** لا «ثابت».
- **كل تقييم ١-٥ موصوف بـ«تقدير الأخصائي وليس قياساً آلياً»** في كل سطح يظهر فيه.
- **تاريخ الميلاد لا يُخمَّن من العمر** — يُقارَنان ويُنبَّه على الفارق.

---

## 4. إصلاحات الأخطاء الكبرى

### 4.1 React Strict Mode — Double-Fire (24 ملف)

**المشكلة:** React يستدعي setState updaters مرتين في Development mode، ما يسبب استدعاء `onComplete` مرتين وإرسال النتيجة مرتين للسيرفر.

**الحلول المطبّقة:**

| النمط | الوصف | الملفات |
|---|---|---|
| `timerRef` + cleanup | تتبع كل setTimeout وإلغاؤه عند unmount | 14 ملف |
| `doneRef` guard | ref يمنع double-fire في الـ updater | TargetTracking, VerbalFluency |
| `advanceRef` guard | نفسه لـ setInterval + setProgress | PhysicalExercise, BodyScan |
| `useMemo` shuffle | ثبات الخيارات بين re-renders | ReadingCards, SpellingBee, PatternPuzzle, IfThen |
| Pre-compute nc/ne | إصلاح stale closure قبل setState | IfThen, PatternPuzzle, LogicSort, ListeningComprehension, SpellingBee, SequenceTap |
| `lightTimerRef` | للـ nested timeout داخل setInterval | FingerGym |
| `timerIds[]` array | لتتبع setTimeouts متعددة | SequenceTap, SequenceMemory |

**الملفات المُصلحة (24):**
TargetTracking, PhysicalExercise, ReadingCards, LetterMatch, EmotionCards, EmotionVolume, IfThen, PatternPuzzle, PatternMatch, MoodMeter, WordBuilder, LogicSort, FingerGym, ListeningComprehension, VerbalFluency, SpellingBee, BodyScan, PictureWordCards, ImitationMirror, SensoryCheckIn, SequenceTap, SequenceMemory, VisualMatch, VisualSchedule

### 4.2 إصلاحات أمنية

| الخطأ | الإصلاح |
|---|---|
| passwordHash يُرسل للعميل | strip قبل إرسال بيانات Parent |
| PII في analytics endpoint | إزالة الحقول الحساسة |
| homework GET بدون auth | إضافة مصادقة admin |
| studentId غير مُعقَّم | sanitize في assessments POST |
| ثغرات في تسجيل/تسجيل دخول | إغلاق جميع الثغرات |
| IDOR في المواعيد | تحقق من ملكية الموعد |
| Redis failure يكسر الـ app | fault-tolerant sessions |

### 4.3 إصلاحات أخرى

- مؤقت الجلسة كان يُضاعف الوقت عند إعادة الفتح → إصلاح
- NBackTask كان يتجمّد بعد أول جولة → إصلاح (ITI timer كان يلغي نفسه)
- SequenceMemory: ألوان، أرقام، حساب score، feedback → إصلاح شامل
- حفظ الجلسات والتقييمات (data loss صامت) → إصلاح
- مقاطع فيديو طويلة جداً (library) → استبدال بمقاطع ≤30 ثانية

### 4.4 إصلاحات جلسة 13 (المزامنة اللحظية)

| الخطأ | السبب الجذري | الإصلاح |
|---|---|---|
| التمارين مختلفة بين الشاشتين | البذرة تُضبط في useEffect بعد mount فيلتقط الأخصائي بذرة قديمة | حساب البذرة أثناء الرسم عبر ref محروس |
| إعادة التمرين لا تصل للطفل | fetchLive يتفاعل مع تغيّر exerciseId فقط، والإعادة تُبقيه | المراقبة على `(exerciseId + seed)` |
| إجابة الطفل لا تظهر للأخصائي | المؤشر داخل الرأس الذي يُخفى عند التمرين | مؤشر عائم دائم الظهور |
| الكاميرا لا تتحرك | السحب يضبط left دون تحرير right | تحرير `right/bottom` عند السحب |
| الفيديو "شبه محجوب" | PiP يهدم مكالمة Daily عند كل تصغير | إبقاء الاتصال حياً (إخفاء CSS فقط) |
| صناديق الفيديو تُغطّى بشريط المتصفح | كلها في الزاوية السفلى اليسرى (مكان شريط كاميرا المتصفح) | نقلها للزاوية العلوية اليمنى |
| المواعيد الجديدة لا تظهر في "القادمة" | "قادمة" = أي `scheduled` بلا فحص تاريخ، مرتبة من الأقدم، والبقعة تعرض 3 فقط | "قادمة" = مستقبلية فعلاً (تاريخ+وقت ≥ الآن)، الأقرب أولاً، والمتأخرة في قائمة منفصلة |

---

## 5. أمان المشروع — الحالة الراهنة

- ✅ جميع API endpoints محمية بمصادقة
- ✅ لا PII يُرسل لصفحات العميل
- ✅ passwordHash لا يُغادر السيرفر
- ✅ Rate limiting على تسجيل الدخول
- ✅ HMAC-signed sessions (Redis fault-tolerant)
- ✅ IDOR checks على المواعيد والبيانات
- ✅ sanitize على جميع المدخلات الخارجية
- ✅ **لا يُوثَق بأرقام العميل في التقارير** — ملخّص APA يُعاد حسابه في الخادم من الحصص المسجّلة، وأي `apa` في جسم الطلب يُتجاهَل
- ✅ **أسماء المراحل والمؤشرات تُقاطَع مع الخطة المؤلَّفة** فلا يُحقَن نص في وثيقة ولي أمر
- ✅ **حسابات الأطفال الحضوريين بلا كلمة مرور معروفة** — الدخول عبر رابط إعادة تعيين مقصور على المالك
- ✅ `PATCH /api/admin/students/[id]` يقبل التشخيص والشدّة فقط — الاسم وتاريخ الميلاد هوية لا تُعدَّل من هناك
- ✅ `POST /api/parent/practice-result` بفحص IDOR — الولي لا يكتب إلا في ملف أبنائه
- ✅ روبوت الدردشة العام محدود بعدد الرسائل **وحجمها** (1000 حرف/رسالة) — المسار الوحيد غير المُصادَق إلى نموذج مدفوع
- ✅ **أسماء الأشخاص تُعقَّم عند كل مدخل** (`lib/person-name.ts`): حدّ 60 حرفاً + إزالة الأسطر الجديدة ورموز التحكم وعكس الاتجاه — لأنها تصل نصوص الـAI وقوالب البريد ووثيقة الولي المطبوعة
- ✅ كل وصول إلى التخزين المحلي عبر `lib/safe-storage.ts` — لا يرمي إن حجب المستخدم بيانات الموقع
- ✅ **ملاحظات الأخصائي السريرية لا تُرسَل للولي** — تُحذف في الخادم داخل `GET /api/parent/assessment` لا في الواجهة، فلا تبلغ المتصفح أصلاً. ومعها تُحذف `answers` الخام و`assessedByActorId` (قيمة تدقيق داخلية)
- ✅ **مراسلات الولي والأخصائي دائمة** — لا مهلة على الرسالة ولا على قائمة المحادثة ولا على عدّادات «غير المقروء»، وتُقرأ كاملة
- ✅ **الدخول بحساب Google لا يُنشئ حساباً ولا يتجاوز حالة الحساب** — `email_verified` شرط، والحساب الموقوف يبقى موقوفاً، وبريد بلا حساب يُحوَّل لنموذج التسجيل
- ⚠️ **استثناء مقصود في تاريخ الميلاد:** `PATCH /api/admin/students/[id]` يقبله **فقط حين يكون السجل خالياً منه** — *تغيير* تاريخ موجود يبقى مرفوضاً. السبب أن طفلاً بلا تاريخ ميلاد لم يكن له أي مدخل في المنصة كلها، والعمر يحجب مقاييس فعلاً

---

## 6. قواعد العمل الثابتة (Standing Rules)

> هذه القواعد مُفعَّلة دائماً في كل جلسة:

1. **كل تغيير يُدفع مباشرة على `main`** — لا يوجد PR إلا بطلب صريح
2. **لا تسأل قبل الدمج** — نفّذ وادفع مباشرة
3. **أرفق نصيحة مع كل تغيير** — توصية أو تحذير أو اقتراح
4. **لا تعرض أبداً** قيم `.env.local`، `AUTH_SECRET`، أي token
5. **لا تخترع URLs أو Video IDs** — استخدم فقط ما يوفره المستخدم
6. **TypeScript صفر أخطاء** — `npx tsc --noEmit` قبل كل commit
7. **هذا كود إنتاجي حقيقي** يُستخدم مع أطفال في جلسات علاج
8. **كل تنسيق تاريخ/وقت/رقم يمرّ عبر `lib/format.ts`** — لا `toLocale*` بلغة مكتوبة يدوياً، ولا تعريف ثانٍ لـ`localeFor`. والأرقام دائماً غربية (0-9) لا عربية-هندية — لا تكتب رقماً عربياً-هندياً في مصدر، ولا تُنسّق تاريخاً بـ`'ar'` أو `'ar-SA'`؛ استخدم `'ar-u-nu-latn'`. `tests/numerals.test.ts` يحرس الاثنين
9. **`ltr-num` للمحتوى الرقمي الخالص فقط** — يضع `direction: ltr` فينقل الرقم المتصدّر إلى **آخر** السطر العربي (`5 مرة` ← `مرة 5`). لا تضعه على نص يبدأ برقم ثم عربية. `tests/ltr-num.test.ts` يحرسه
10. **كل تنقّل بين خطوات معالج (wizard) يجب أن يكون ذا اتجاهين** — ومن يمسح بيانات المستخدم يسأل أولاً. شريط خطوات غير قابل للنقر = المستخدم محبوس
11. **لا مهلة انتهاء على سجل سريري** — `assessment:` و`apa-record:` و`ld-profile:` و`report:` تُكتب بلا `EX`. الفهارس التي تشير إليها بلا مهلة، فأي مهلة على السجل نفسه تُنتج تاريخاً يقصر بصمت. `tests/redis-encoding.test.ts` يحرسها، ويحرس أيضاً أن `redis.set` لا يُمرَّر إليه نص مُسلسَل مسبقاً (فهو يُسلسل داخلياً)
12. **لا تشتقّ الملكية من قائمة** — `appointment.parentId` لا «هل هو ضمن قائمة مواعيده». القائمة قد تكون مبتورة فيُحجب المستخدم عن بياناته
13. **سجل الطفل يُقرأ كاملاً لا بنافذة** — `reports:student:` و`assessments:student:` و`appointments:parent:` بـ`-1`. قراءة مقيَّدة لسياق نموذج فقط، وتُوسَم `prompt-window`. `tests/redis-encoding.test.ts` يحرسها
14. **كل استدعاء شبكة خارجي يُفحص رده** — `fetch` بلا `res.ok` يحوّل الفشل إلى نجاح صامت. ينطبق بالذات على البريد: المستدعي يبني عليه قراراً (مفتاح عدم تكرار، رسالة نجاح للمستخدم)
15. **كل نوع سجل جديد يُضاف إلى `lib/backup-types.ts`** — النسخة الاحتياطية تُقاس بما تحويه يوم الكارثة لا بما يُسمّى. و**كل أرشيف مشفَّر يلزمه أداة فتح تُشحن معه**
16. **كل مقياس جديد يُعلن اتجاهه** — أعراضٌ (الانخفاض تحسّن) أم قدرةٌ (الارتفاع تحسّن)؟ مقياس قدرة لا يُسجَّل في `ABILITY_SCALES` **يقلب كل قراءاته بصمت**
17. **لا درجة ذكاء ولا مئين ولا درجة كلية على مهام الأداء** — لا عيّنة تقنين خلفها. المقارنة بالطفل نفسه فقط، ولا تصلح لقرار أهلية أو تصنيف مدرسي
18. **البيانات المحفوظة ليست بيانات مرئية** — قبل اعتبار ميزة مكتملة، افتح الشاشة التي سيفتحها المستخدم فعلاً وتأكّد أنها تعرضها. أكثر عيوب هذا المشروع كان «يُحفَظ ولا سطح يقرؤه»

---

## 7. Stack التقني

| التقنية | الاستخدام |
|---|---|
| Next.js 15 (App Router) | Framework الأساسي (رُقّي من 14 → 15.5.20 لإغلاق ثغرات أمنية) |
| TypeScript | كل الكود |
| Tailwind CSS | التصميم |
| Redis (Upstash) | جلسات المصادقة، حالة الجلسة اللحظية، التخزين المؤقت |
| Resend | إرسال الإيميلات |
| Daily.co (Prebuilt Iframe) | مكالمات الفيديو المتبادلة في الجلسة |
| Pusher (pusher / pusher-js) | المزامنة اللحظية (إيقاظ + Redis مصدر الحقيقة) |
| Web Audio API | نغمات التغذية الراجعة + توليد الموسيقى/الضوضاء |
| Web Speech API | TTS في التمارين |
| React useRef/useMemo | ثبات الحالة والبذور في التمارين |
| Vitest | 398 اختبار وحدة في 35 ملفاً (`tests/`) — تُشغَّل قبل كل دفع |
| @react-pdf/renderer + Tajawal | توليد تقرير الحقيبة PDF في الخادم (خط مضمَّن base64) |
| Playwright (تحقّق فقط) | تشغيل التطبيق فعلياً والتحقّق من كل ميزة قبل الدفع — ليس تبعية إنتاج |

---

## 8. هيكل التمرين — النمط الصحيح

كل تمرين يجب أن يتبع هذا النمط:

```tsx
'use client'
import { useState, useRef, useEffect } from 'react'
import type { ExerciseResult, ExerciseProgressUpdate } from '@/lib/types'
import { createRng, shuffleWithRng } from '@/lib/seeded-random'

interface Props {
  onComplete: (r: ExerciseResult) => void
  onCancel: () => void
  studentAge: number
  difficulty?: 1|2|3
  seed?: number                                        // بذرة مشتركة → نفس المحتوى على الشاشتين
  onProgress?: (p: ExerciseProgressUpdate) => void     // تغذية راجعة لحظية للأخصائي/الطفل
}

export default function MyExercise({ onComplete, onCancel, difficulty = 1, seed, onProgress }: Props) {
  // ✅ RNG حتمي من البذرة المشتركة — بدل Math.random المستقل
  const rng      = useRef(createRng(seed ?? Date.now())).current
  const startRef = useRef(Date.now())
  const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null)

  // ✅ إلغاء التايمر عند unmount
  useEffect(() => () => { if (timerRef.current) clearTimeout(timerRef.current) }, [])

  // ✅ shuffle حتمي (نفس الترتيب على الجهازين)
  const choices = shuffleWithRng(rng, rawChoices)

  function handleAnswer(answer: string) {
    // ✅ pre-compute قبل setState لتجنب stale closure
    const nc = correct + (isCorrect ? 1 : 0)
    const ne = errors  + (isCorrect ? 0 : 1)
    if (isCorrect) setCorrect(nc)
    else           setErrors(ne)

    // ✅ بثّ التقدم اللحظي (قيم مُحسبة مسبقاً، لا state قديم)
    onProgress?.({ answered: idx + 1, total, correct: nc, errors: ne, lastCorrect: isCorrect })

    // ✅ تتبع التايمر
    timerRef.current = setTimeout(() => {
      if (isLastQuestion) {
        onComplete({ score: Math.round((nc / total) * 100), errors: ne, ... })
      }
    }, 1000)
  }
}
```

> **ملاحظة:** لا تستخدم `Math.random()` في التمارين إطلاقاً — استخدم `rng()` أو مساعدات `lib/seeded-random.ts` حتى يبقى المحتوى متطابقاً بين شاشة الأخصائي والطفل.

---

## 9. سجل الجلسات

| الجلسة | ما تم |
|---|---|
| الأولى | بناء الهيكل الأساسي، Auth، Landing Page |
| الثانية | Admin Dashboard كامل، Payment، Parent Portal |
| الثالثة | Session Platform، تمارين أساسية |
| الرابعة | تصميم Landing بالكامل، Bilingual |
| الخامسة | إصلاح أمني شامل، إيميلات، مراسلة |
| السادسة | Session UX: إخفاء Chrome، إعادة تشغيل، خريطة تقدم |
| السابعة | ProgressMap (Duolingo)، adaptive difficulty، session intelligence |
| الثامنة | إصلاحات بنية التمارين، عسر القراءة، SensoryCheckIn |
| التاسعة | توسيع محتوى التمارين، NumberSearch، تحرير مدة المراحل |
| العاشرة | إعادة تصميم Readiness Screen، Light Theme للجلسة |
| الحادية عشرة | استخراج مكونات session/page.tsx (6 مراحل refactor) |
| الثانية عشرة | إصلاح React Strict Mode bugs في 24 تمرين |
| الثالثة عشرة | **المزامنة اللحظية الكاملة:** Pusher (إيقاظ + Redis)، Daily prebuilt iframe، بذرة عشوائية مشتركة عبر 63 تمرين، رؤية إجابات الطفل لحظياً (60 تمرين + لوحة أخصائي)، انفجارات التشجيع، خارطة تقدم الطفل + نجوم + تغذية راجعة صوتية موحّدة، تقييم جاهزية يجيبه الطفل، تقوية الموثوقية (إعادة محاولة الفيديو + إعادة مزامنة عند الاتصال)، إصلاح المواعيد القادمة |
| الرابعة عشرة | **حلقة المصداقية (تقييم → خطة → تقرير):** تقارير مبنية على بيانات اللعب الفعلية + مقياس تحسّن قابل للقياس، **شارة تحسّن موثّقة** («+X٪ دقّة») في الوثيقة المطبوعة للوالد، **خطة مقترحة قائمة على قواعد** من التقييم الأول (`lib/assessment-plan.ts`: عدد حصص/أسبوع + عدد أسابيع + مسار المجالات المستهدفة مرتّباً + موعد إعادة تقييم) تُحسب في الخادم وتُعرض للوالد والأخصائي وتُغذّي مولّد البرنامج بالـAI (كل التوصيات بدل 3)، endpoint أدمن لعرض تقييمات الوالد للأخصائي |
| الخامسة عشرة | **رفع المصداقية العلمية — تبنّي مقياس فاندربيلت (NICHQ Vanderbilt Parent):** استبدال الاستبيان المخصّص بمقياس فرز ADHD **معتمد ومجاني** (`lib/vanderbilt-data.ts`): بنود DSM الفعلية (٩ قلة انتباه + ٩ فرط حركة/اندفاعية + ٨ سلوك معارض)، مقياس التكرار الرسمي ٠–٣، قسم **الأثر الوظيفي (Performance)** ١–٥، وقاعدة التسجيل الرسمية (≥٦ أعراض «غالباً/دائماً» في مجال **مع** تأثّر وظيفي = فرز إيجابي). التسجيل **كله في الخادم** (لا تلاعب)، يُنتج النمط (قلة انتباه/فرط حركة/مشترك/دون العتبة) + شدّة + خطة (`buildPlanFromVanderbilt`). أُطّر صراحةً كأداة فرز لا تشخيص. صفحة التقييم أُعيد بناؤها (أعراض → أداء → نتيجة + خطة)، ثلاثية اللغة عبر خريطة محلية (بنود عربية أو إنجليزية أصلية). ٧ اختبارات حدّية جديدة (٨٩ إجمالاً). **المصداقية الآن حقيقية:** الادّعاء «مبني على مقياس معتمد» أصبح صحيحاً |
| السادسة عشرة | **إصلاحات أمنية شاملة (فحص Aikido) + ترقية كبرى:** إزالة كلمة مرور الأدمن المسرّبة من `PLATFORM_REFERENCE.md` (دُوِّرت في Vercel)، إصلاح Open Redirect في تسجيل دخول الوالد (`lib/safe-redirect.ts`)، ترقية nodemailer 6→9 (٧ ثغرات)، وترقية **Next.js 14 → 15.5.20 + React 18 → 19** لإغلاق كل ثغرات Next.js المتبقية (cache-poisoning/DoS/XSS) — هجرة async params عبر ٢٩ ملفاً بالـcodemod الرسمي + تخفيف no-store على الـmiddleware. من ٣ ثغرات (٢ عالية) إلى ٢ متوسطة بناء فقط. تحقّق كامل بالتشغيل الحي (٩٦ اختباراً) |
| السابعة عشرة | **دليل حصص النشاط البدني المعدّل (APA) موصولاً بمكتبة التمارين:** صفحة `/dashboard/apa-planner` — خطط ADHD/ASD × ٤ فئات عمرية مع عدّاد حصة ٦٠ دقيقة حي، تُفتح مضبوطة مسبقاً على بيانات الطفل من الحقيبة (`?cond=&age=&name=`). **كل مرحلة موصولة بتمارين حقيقية من مكتبة المنصة الـ٤٥** (`lib/apa-exercise-link.ts`): يعرض الأخصائي خطوات البروتوكول وهدفه النفسي وموانع استعماله داخل بطاقة المرحلة نفسها — مصدر واحد للحقيقة، لا نسخة ثانية من المحتوى. الصفحة **مكوّن خادم** يقتطع الكتالوج (١١٥KB) فلا يصل للمتصفح كاملاً. ٨ اختبارات انجراف (`tests/apa-exercise-link.test.ts`) تمنع أي تمرين خارج الفئة العمرية أو خارج التشخيص — وقد أمسكت فعلاً بخطأين: تنفس صندوقي (١٢+) لطفل ٦-٩، ودائرة قوة (١٨-٢٢) لفئة ١٢-١٥ |
| الثامنة عشرة | **إدخال حصص النشاط البدني في حلقة المصداقية:** كانت حصة APA مدتها ٦٠ دقيقة تُنتج **صفر بيانات** — مؤشرات الخطة لا يسجّلها شيء، فتقرير الولي لا يذكر عنها حرفاً. أُضيف: نموذج «تسجيل نتائج الحصة» في المخطط (الطفل + المراحل المنفّذة + تقييم ١-٥ لكل مؤشر من مؤشرات تلك الفئة + المدة + ملاحظات)، مسار `/api/admin/apa-sessions`، تجميع نقي (`lib/apa-record.ts`) يُنتج عدد الحصص/الدقائق/نسبة إنجاز المراحل + اتجاه كل مؤشر، وقسم في وثيقة الولي (ar/en/fr) وفي مولّد الملخص بالـAI. **الملخص يُعاد حسابه في الخادم** من الحصص المسجّلة فقط — أرقام APA المرسلة من العميل تُتجاهَل تماماً، وأسماء المراحل والمؤشرات تُقاطَع مع الخطة المؤلَّفة فلا يمكن حقن نص في تقرير ولي. حصة واحدة تُوصف «لا يمكن استنتاج اتجاه بعد» لا «ثابت». كل التقييمات موصوفة صراحةً بـ«تقدير الأخصائي وليس قياساً آلياً». ٢٤ اختبار وحدة + تحقّق حي كامل (١٦ فحص خلفية + ١٠ واجهة أخصائي + ٩ واجهة ولي) |
| التاسعة عشرة | **حسابات الأطفال الحضوريين (بلا تسجيل ولا دفع):** تدقيق كشف أن **بوابة الولي لا تُقفل بالدفع أصلاً** — تُقفل بـ`subscriptionStatus` فقط، وتفعيل البريد وحده يجعل الحساب `active`؛ و**لا ميزة واحدة محجوبة بالباقة** (`subscriptionPlan` للعرض فقط). الفجوة الحقيقية كانت: لا سبيل لإنشاء حساب من اللوحة (`/api/admin/clients` كان GET/PATCH/DELETE بلا POST، وزر «إضافة مشترك» يحيل إلى `/register` العام). أُضيف: `accountType: 'online' \| 'in-person'` على `Parent` (اختياري — الغياب = `online` للتوافق الخلفي، يُقرأ عبر `lib/account-type.ts` لا بمقارنة مباشرة)، `POST /api/admin/clients` ينشئ الولي + الطفل دفعة واحدة (`active`، `expiry: null` فلا يمسّه cron الاشتراكات، كلمة مرور عشوائية لا يراها أحد — العائلة تدخل عبر رابط إعادة التعيين المقصور على المالك)، نموذج في اللوحة **يطلب البريد مرتين** لأنه المفتاح لملف طفل سريري، وإخفاء **كل** سطح دفع عن العائلة الحضورية (شارة الباقة ← «متابعة حضورية»، زر تغيير الباقة، مدخل القائمة، و**صفحة الأسعار نفسها محروسة ضد الرابط المباشر** — لا تُرسم قبل معرفة هوية الزائر، وفشل الجلب يُبقيها محجوبة). فُصلت مؤشرات اللوحة فلا يتضخّم عدد «العملاء النشطين» بحسابات مجانية. ١٠ اختبارات وحدة + ٢٠ فحص API + ١٩ فحص واجهة + ٥ فحوص عدم-انحدار لعميل يدفع. **الدخول بالبريد (magic link) مؤجَّل عمداً** — لا يُفتح باب مصادقة جديد على ملفات أطفال لمشكلة لم تُثبت بعد |
| العشرون | **إغلاق حلقة التقييم + دقّة العمر + تصحيح ما بعد التقييم:** (١) كانت الحقيبة تسمح بتقييم كامل لطفل غير مسجَّل ثم **يضيع كل شيء بصمت** عند إغلاق الصفحة — أُضيف تحذير صريح وزر «إنشاء حساب حضوري لهذا الطفل» داخل خطوة الربط، يفتح النموذج مُعبّأً ويربط الطفل فوراً (`onLinked`) فيبدأ الحفظ التلقائي. (٢) **حقل تاريخ ميلاد في الحقيبة** والعمر يُحسب منه ويصير للقراءة فقط؛ وأُنشئ `lib/age.ts` بحساب **تقويمي دقيق** بدل القسمة على 365.25 التي كانت في ثلاثة أماكن وتنحرف بيوم حول عيد الميلاد — وهذا يهمّ لأن مقياس صعوبات التعلّم محجوب تحت ٨ وعتبة DSM-5 تنزل عند ١٧؛ ويرفض أيضاً تواريخ لا وجود لها (`2025-02-30` كان JS يحوّله بصمت لـ٢ مارس). (٣) **ملاحظة المستخدم:** النموذج كان يطلب «التشخيص» **قبل** التقييم وهو ما يناقض موقف المنصة «فرز لا تشخيص» — ووُجد أنه **غير قابل للتعديل أبداً** بعد الإنشاء. أُعيدت تسميته «مجال الاهتمام المبدئي» بافتراض «غير محدد بعد» وتنويه «ليس تشخيصاً»، وأُضيف `PATCH /api/admin/students/[id]` + محرّر في ملف العميل لتصحيحه بعد التقييم (يقبل التشخيص والشدّة فقط — الاسم وتاريخ الميلاد هوية). ١٣ اختبار عمر (١٨٠ إجمالاً) + ٨ فحوص API + ١١ فحص واجهة |
| الحادية والعشرون | **إيصال التقارير + دور المنصة وإخلاء المسؤولية:** تدقيق كشف ثلاث فجوات في وصول التقرير للعائلة: (١) **لا بريد يُرسَل عند إنشاء تقرير** — التقرير يبقى في البوابة حتى يصادف الولي فتحها، وعائلة حضورية لا سبب لديها لفتحها أصلاً؛ أُضيف `reportReadyEmail` (اسم الطفل والفترة ورابط فقط — **لا محتوى سريري في البريد**، فالبريد ليس قناة سرّية) يُرسَل من `POST /api/admin/reports` بلا أن يُفشل حفظ التقرير. (٢) **الـcron الأسبوعي كان يتخطّى كل طفل حضوري بصمت** لأن شرط «يوجد نشاط» كان `totalPlays > 0` وهو صفر دائماً لمن لا يلعب على الشاشة؛ صار يحتسب حصص APA المسجّلة أيضاً، والبريد يذكرها. (٣) **وثيقة الولي كانت بلا أي إخلاء مسؤولية** رغم وجوده في PDF الحقيبة — وهي الوثيقة التي تُطبَع وتُعرض في المدرسة. أُضيف قسم **«طبيعة هذه الوثيقة»** داخل الكتلة القابلة للطباعة بثلاث لغات: دور المنصة (توثيق لا تشخيص)، دور الأخصائي (اجتهاد مهني ومسؤوليته)، المقاييس (أدوات فرز — نتيجة إيجابية = حاجة لتقييم لا تشخيص)، إخلاء مسؤولية صريح (ليست وثيقة طبية ولا قانونية ولا مدرسية رسمية + «لا تنتظر التقرير القادم عند التدهور») وملاحظة سرّية. ٦ فحوص API + ١٢ فحص واجهة شملت الطباعة |
| الثانية والعشرون | **تمارين المنزل تُسجَّل فعلاً + إيقاع التقارير:** تدقيق كشف أن `/parent/practice` كان **يُظهر النتيجة ثم يفقدها** — `handleComplete` يضع النتيجة في state فقط، و`/api/game-results` مقصور على الأدمن، فلا شيء مما يلعبه الطفل في المنزل يصل لأي تقرير. أُضيف `POST /api/parent/practice-result` (مصادقة ولي + **فحص IDOR**: الطفل يجب أن يكون من أبنائه وإلا 404، وتحديد معدّل ١٢٠/ساعة)، ويُوسَم كل صف بـ`sessionId: 'home-practice'` ليُميَّز عن جلسة أشرف عليها الأخصائي. النتيجة تصل تلقائياً إلى `getStudentGameHistory` ← زر «احسب من بيانات الطفل الفعلية» في نموذج التقرير. **وُسِّعت تمارين المنزل من ١٠ إلى ٢٠** مع استثناء **بطاقة القياس عمداً** (SpanExtension، الانتباه المستمر، البحث البصري، الذاكرة السمعية) — تمرينها في المنزل يرفع النتيجة ويُتلف خط الأساس الذي يقارن عليه الأخصائي. وأُضيف عدّاد **«حصص منذ آخر تقرير»** في نموذج التقرير (تنبيه عند ≥٢) لأن إيقاع التقارير لكل طفل مختلف. ٧ فحوص API شملت محاولة ولي الكتابة في ملف طفل غيره + ٤ فحوص واجهة |
| الثالثة والعشرون | **PSC-17 — سدّ البُعد الانفعالي + توضيح إيصال البريد:** كانت البطارية تغطي ADHD والتوحد وصعوبات التعلم والذاكرة والانتباه، و**لا تغطي البُعد الداخلي إطلاقاً** (الحزن، اليأس، تدنّي تقدير الذات، القلق، فقدان الاستمتاع) وهو شائع مع ADHD والتوحد ويغيّر الخطة. أُضيف **PSC-17** (Gardner et al., 1999 — مقياس فرز منشور ومجاني): البنود الـ١٧ بصياغتها الإنجليزية الأصلية + ترجمة عمل عربية **موصوفة صراحةً بأنها ليست نسخة عربية مُقنّنة**، مقياس ٠-٢ الرسمي، المجالات الثلاثة بمجموعاتها المنشورة، والعتبات المنشورة (كلي ١٥ · داخلي ٥ · انتباه ٧ · خارجي ٧). **التسجيل في `lib/psc17-data.ts` ويرفض النموذج الناقص** — مجموع ناقص يبدو فرزاً سلبياً مطمئناً وهو ليس كذلك. مجال «الداخلي» مربوط بتمارين تهدئة فقط مع توصية إحالة صريحة، لأن **لا تمرين في هذا الكتالوج يعالج المزاج المنخفض**. ١٨ اختباراً شملت حدود العتبات والمثال المنشور (كلي ١٥/انتباه ٩) وحالة «مجموع منخفض يخفي ضائقة داخلية». وأُضيف إظهار **حالة إرسال البريد** بعد حفظ التقرير بدل تركه صامتاً |
| الرابعة والعشرون | **جعل PSC-17 قابلاً للاكتشاف:** المستخدم لم يجد المقياس — والسبب حقيقي: `CONCERN_TO_SCALE` كان يربط ثلاثة مخاوف فقط (توحد/ADHD/صعوبات تعلّم)، فـPSC-17 كان موجوداً في قائمة الخطوة الثانية لكن **لا شيء في الخطوة الأولى يشير إلى وجوده**. أُضيف مخوف رابع «مؤشرات انفعالية أو سلوكية» يوجّه إليه، مع نصيحة إحماء خاصة («بنود الحالة الانفعالية تصف ما يحدث في البيت — دع ولي الأمر يجيب عنها بنفسه، فلن ترى معظمها في حصة واحدة»). ٧ فحوص حيّة |
| الخامسة والعشرون | **تنشيط سلوكي — ما يمكن فعله فعلاً للمزاج المنخفض:** السؤال كان «هل يمكن إضافة تمارين تعالج المزاج المنخفض». الجواب الصادق: **لا شيء هنا يعالج الاكتئاب** — يعالجه مختص نفسي مؤهّل. لكن **التنشيط السلوكي** (جدولة أنشطة ممتعة، أغلبها حركية) هو الآلية ذات الدليل الحقيقي الواقعة **داخل نطاق أخصائي النشاط البدني المعدّل**. أُضيف `MoodActivation`: الطفل يقيس مزاجه ← يختار نشاطاً من قائمة قصيرة (٦ خيارات؛ الاختيار أصعب مع المزاج المنخفض) ← مؤقت قصير ← يقيس مزاجه مجدداً. **يقيس ولا يدّعي:** يكشف أي الأنشطة ترفع مزاج **هذا الطفل تحديداً** (`bestActivities` يتجاهل ما جُرِّب مرة واحدة — حادثة فردية ليست نتيجة). **قرارات نبرة مقصودة:** لا تهنئة على التحسّن إطلاقاً (الطفل المُكافأ على الشعور الأفضل يتعلّم *الإبلاغ* عنه فتنهار قيمة القياس)، ولا لوم عند عدم التحسّن، والرسالة عند الانخفاض توجّهه لشخص («أخبِر من يهتم بك») لا لمحاولة أشد. مزاج عند الحد الأدنى أو منخفض = `needsAttention` للأخصائي. في توصيات PSC-17 موصوف صراحةً **«بالتوازي مع الإحالة لا بديلاً عنها»**. ٢١ اختبار وحدة شملت النبرة نفسها + ١٣ فحصاً حياً. **عيب أمسكه الفحص:** المضيف يُغلق التمرين فور `onComplete` فكان الطفل لا يرى الرسالة أبداً — أُجِّل الإبلاغ حتى يضغط «تم» |
| السادسة والعشرون | **الأرقام الغربية في كل المنصة + تحديث السجل:** الأرقام كانت تظهر عربية-هندية في الصفحة الرئيسية وصفحة الدفع والجولة. **مصدران منفصلان، وكلاهما أُغلق:** (1) أرقام مكتوبة حرفياً في المصدر — 16 سطراً في 7 ملفات؛ (2) تنسيق التواريخ بـ`'ar'` و`'ar-SA'` و`'ar-EG'` الذي **يترك شكل الرقم لمتصفح المستخدم** — يعطي أرقاماً غربية في Node وعربية-هندية في متصفحات أخرى، فلا يُكتشف بالفحص المحلي. وُحِّد الكل على `'ar-u-nu-latn'` (عربية بأرقام لاتينية) في 8 نسخ من `localeFor` وفي `/session/[id]` وسجل المراجعة — **مع استثناء `lib/speech.ts` عمداً**: `'ar-SA'` هناك وسم صوت للنطق لا تنسيق أرقام، وتغييره يكسر TTS العربي. `tests/numerals.test.ts` (7 اختبارات) يمنع عودة الاثنين ويتحقق أن الوسم يُنتج أرقاماً لاتينية فعلاً مع إبقاء أسماء الشهور عربية. 6 فحوص حيّة على الصفحة الرئيسية والدفع والجولة واللوحة. **226 اختباراً إجمالاً** |
| السابعة والعشرون | **تدقيق صنف «يبدو سليماً عندي وخطأ عند المستخدم»:** بعد إصلاح الأرقام، فُحص الصنف كلّه فظهرت **ثلاثة عيوب من النوع نفسه**: (1) **20 موضعاً تُنسّق التواريخ بـ`'fr-FR'` داخل واجهة عربية** — ولي أمر عربي كان يقرأ «16 sept.» في **وثيقته المطبوعة نفسها**؛ ومنها موضعان مخفيان يُسندان اللغة لمتغيّر (`const locale = lang === 'ar' ? 'fr-FR' : …`) فلم يلتقطهما المسح النمطي، وواحد بـ`'ar-TN'` في يوميات الطالب. (2) **انزياح يوم كامل**: حقل تاريخ فقط مثل `2026-09-01` يُفسَّر منتصف ليل UTC فيُعرض **31 أغسطس** لكل من يعيش غرب UTC — غير مرئي في قطر وتونس، خاطئ لعائلة في الأمريكتين، ويقع على **فترة تقرير الولي**. (3) **رابط واتساب ميت**: أربعة احتياطات مختلفة لنفس الرقم، منها رقم وهمي في زر الصفحة الرئيسية ومحادثة الولي، وصيغة `+974…` التي لا يقبلها `wa.me`. أُنشئ **`lib/format.ts`** مصدراً واحداً (`localeFor`، `formatDateOnly` مثبّت على UTC، `formatDateTime`/`formatTime` بتوقيت الزائر، `whatsappNumber`/`whatsappLink` ينظّفان الرقم)، وحُذفت **8 نسخ متكرّرة من `localeFor`** كانت تنجرف كلٌّ على حدة. 17 اختباراً جديداً + حارسان يمنعان لغة فرنسية خارج الواجهة الفرنسية وأي تعريف ثانٍ لـ`localeFor`. تحقّق حي **من متصفح على توقيت UTC-4** أثبت أن بداية الفترة صارت «1 سبتمبر» لا «31 أغسطس». **243 اختباراً** |
| الثامنة والعشرون | **مواصلة التدقيق — عيبان حقيقيان وثلاثة أصناف نظيفة:** (1) **التخزين المحلي بلا حماية في 15 موضعاً، أخطرها `LanguageProvider`** الذي يعمل على **كل صفحة في الموقع**: زائر حاجب لبيانات الموقع كان `getItem` يرمي عنده `SecurityError` فيُسقط المزوّد و**تظهر له صفحة بيضاء** لا صفحة بلغة خاطئة. أُنشئ `lib/safe-storage.ts` (قراءة تُرجع البديل، كتابة تُرجع `false`، يغطي امتلاء التخزين وJSON التالف وغياب `window` في الرسم الخادمي) وطُبِّق على المواضع كلها. **أُثبت العيب قبل الإصلاح وبعده بمتصفح حقيقي كل استدعاءات تخزينه ترمي.** (2) **روبوت الدردشة العام كان يحدّ عدد الرسائل لا حجمها** — طلب واحد يحمل ميغابايتات إلى نموذج مدفوع على مفتاح الأكاديمية، والحدّ الزمني (20/ساعة) لا يمنع الفاتورة؛ استُخرج `lib/chat-input.ts` بحدّ 1000 حرف/رسالة ورفض أي `role` غير `user`/`assistant`. **وثلاثة أصناف فُحصت ووُجدت نظيفة ولم تُلمَس:** `Math.random` في التمارين (صفر)، قسمة على مصفوفة فارغة في واجهات المستخدم (كلها محروسة)، و`setInterval` بلا تنظيف (صفر). 21 اختباراً جديداً |
| التاسعة والعشرون | **فحص الكاميرا والصوت في الجلسة — عيبان صامتان:** (1) **موسيقى الطفل كانت تُنشأ ولا تُستأنف أبداً.** `startNoiseEngine` ينشئ `AudioContext` بلا `resume()`: على شاشة الأخصائي يعمل لأنه ينطلق من **ضغطة زر**، أما على شاشة الطفل فينطلق من **حدث لحظي بلا أي لمسة** — وسياسة التشغيل التلقائي تُنشئه «معلّقاً» فيبقى صامتاً، بينما شاشة الأخصائي تعرض الموسيقى «تعمل». طفل جلس في «موسيقى تهدئة» لم تُشغَّل قط. أُضيف `unlock()`: يحاول `resume()` فوراً، وإن رفض المتصفح يُسلّح مستمعاً لمرة واحدة يبدأ الصوت عند أول لمسة من الطفل، و`NoiseHandle.audible()` يكشف الحالة الحقيقية. ونفس العلاج لمسار الرابط المخصّص: `audio.play().catch(() => {})` كان يبتلع الرفض بصمت ← `playAudioUnlocked`. (2) **رفض إذن الكاميرا كان يُبتلع عمداً** في `DailyVideoCall` بحجة «المكالمة تتصل رغمه» — صحيح، لكن الأخصائي يرى **مربعاً أسود بلا أي تفسير**. فُصل الآن: فشل الاتصال يبقى خطأً أحمر بزر إعادة اتصال، والجهاز المحجوب يظهر كتنبيه سفلي واضح لا يغطي فيديو الطرف الآخر، بثلاث حالات مميّزة (محجوب، مستخدم من تطبيق آخر، غير موجود). **حدّ التحقق بصراحة:** لم أستطع إعادة إنتاج الحالة «المعلّقة» في متصفح بلا جهاز صوت — يُبلّغ `running` دائماً؛ فتحقّقت من آلية الاستئناف على سياق عُلِّق يدوياً، والسلوك موثّق في سياسة التشغيل التلقائي وتفرضه Safari/iPad بصرامة |
| الثلاثون | **تدقيق بقية المنصة — خمسة أصناف نظيفة وعيب وصول واحد:** فُحصت مناطق لم تُمسّ من قبل و**معظمها سليم**: حدود الأخطاء تحمي شاشتي الأخصائي والطفل معاً، وسقوط Redis يُرجع قيماً فارغة لا يُسقط التطبيق، وفقدان البيانات محروس بـ`beforeunload` + مسودة لا تُحذف إلا بعد حفظ ناجح، ولا تسريب لمستمعي الأحداث، وكل `createObjectURL` يُبطَل. **العيب الوحيد:** 121 حقل إدخال بلا اسم وصول — `<label>` مرئي بجانب `<input>` بلا `htmlFor`/`id`، فقارئ الشاشة والتحكم الصوتي يريان حقلاً بلا اسم (والـ`placeholder` ليس تسمية: يختفي عند الكتابة). رُبط **12 حقلاً** في الأسطح التي يملؤها مستخدم حقيقي (بوابة الولي، الدخول، التسجيل، الدفع)؛ و**تُركت 69 في لوحة الأخصائي عمداً** — مستخدمها واحد مُبصر، وإعادة كتابة آلية هناك مخاطرة بلا مستفيد. أُثبت الإصلاح بمتصفح: كل حقل يُرجع اسمه، والضغط على التسمية يُركّز الحقل. (تفضيلات الوصول القائمة — حركة مخفّضة، خط عسر القراءة، تباين عالٍ، و`prefers-reduced-motion` — فُحصت ووُجدت سليمة) |
| الحادية والثلاثون | **حقن التعليمات عبر الأسماء — ثغرة أقرّ بها الكود ولم يسدّها:** تعليق في `register/route.ts` يقول صراحةً إن مدخلات التسجيل العام «تصل بلا تهريب إلى نصوص الـAI وقوالب البريد فيجب ألّا تحمل نصاً عشوائياً» — لكن الحماية طُبِّقت على **`diagnosis` وحده**، بينما **الأسماء بجانبه بـ`.trim()` فقط: بلا حدّ طول وبلا تصفية**، وهي تدخل فعلاً نصوص المسارات الثلاثة. ولي أمر يسجّل طفلاً باسم يحوي كتلة تعليمات يمكنه التأثير على **الملخّص الذي يرسله الأخصائي لعائلة**، والاسم غير المحدود يُضخّم كل نص AI (تكلفة) ويُخزَّن بلا سقف. أُضيف `lib/person-name.ts`: حدّ 60 حرفاً، وإزالة الأسطر الجديدة ورموز التحكم و**رموز عكس الاتجاه** (تُعيد ترتيب النص حولها بصرياً فيمكن لاسم أن يُعيد كتابة سطر في تقرير سريري)، مع إبقاء العربية بتشكيلها و«Jean-Pierre» و«O'Brien» سليمة. طُبِّق على التسجيل العام و`PATCH /api/parent/me` وإنشاء العميل الحضوري، ويُرفض اسم يتبخّر بعد التعقيم بدل تخزينه فارغاً. **وطبقة ثانية:** سطر في نصوص الـAI الثلاثة يخبر النموذج أن الأسماء والملاحظات **بيانات لا تعليمات**. **حدّ صريح:** التعقيم يُسطّح الحقن ولا يمحوه — جملة قصيرة تبقى ممكنة داخل 60 حرفاً؛ ولهذا الطبقة الثانية، ولهذا يبقى الأخصائي هو من يراجع الملخّص قبل حفظه. 11 اختباراً + 5 فحوص حيّة |
| الثانية والثلاثون | **التقرير الأسبوعي كان يكذب على الأهل + رابطان ميتان في البريد:** `byWeek` لا يحوي إلا الأسابيع التي **لعب** فيها الطفل، والبريد كان يقرأ «هذا الأسبوع» بأنه `byWeek[length-1]` — أي آخر أسبوع فيه بيانات. شُغّل الكود القديم على طفل آخر لعبه قبل **ثلاثة أسابيع**، فطبع حرفياً «**9 لعبة هذا الأسبوع · +4% تحسن الأداء**»: رقمان حقيقيان منسوبان لأسبوع لم يحدث فيه شيء، ويُرسَلان كل أحد بلا نهاية لأن بوابة الإرسال كانت `totalPlays > 0` وهي حصيلة العمر كله. أُنشئ `lib/week.ts` (مفاتيح أسابيع تبدأ الاثنين، **بتوقيت UTC بالكامل** — التطبيق القديم كان يخلط `getDay()` المحلي بـ`toISOString()` فيمكن أن يصير مفتاح «الاثنين» يوم أحد خارج UTC)، والأسابيع تُطلَب **بمفتاحها** فالأسبوع الغائب يقرأ صفراً لا أسبوعاً أقدم. والبريد صار يقول «لا نشاط على المنصة هذا الأسبوع» و«لا مقارنة متاحة» بدل `0%` (تُقرأ «لا تقدّم»)، والأرقام التراكمية موسومة «منذ البداية». وأُصلح **رابطان ميتان**: البريد الأسبوعي كان يقرأ `NEXTAUTH_URL` (متغيّر لا وجود له في مشروع مصادقته HMAC)، وبريد الدفع `NEXT_PUBLIC_APP_URL ?? ''` فينتج `href="/dashboard/payments"` — رابط نسبي لا شيء يُحلّه داخل بريد. وُحِّدا في `lib/base-url.ts` مع احتياط `VERCEL_URL` فلا يموت رابط ثانية. 19 اختباراً |
| الثالثة والثلاثون | **تقرير الحقيبة لم يكن يصل العائلة إطلاقاً (بلاغ المستخدم):** التقييم يُحفَظ فعلاً («تم حفظه بشكل دائم» صادقة)، والحقيبة تعرضه كوثيقة عنوانها «**وثيقة سرية خاصة بولي الأمر** والفريق المختص» — ومع ذلك **لا شاشة واحدة عند الولي تعرضه**. صفحة التقارير تقرأ `getStudentReports` (`ProgressReport` تُنشأ يدوياً من مسار آخر) فلا تُظهره أبداً؛ و`GET /api/parent/assessment` كان **يُرجع البيانات فعلاً** لكن قارئه الوحيد صفحة استبيان فاندربيلت، وتعرضها **ثلاثة أسطر**: تاريخ وشارة شدّة، بلا اسم مقياس ولا درجات ولا توصيات ولا إشارة إلى أن أخصائياً أجراها. أي: البيانات موجودة، والمسار موجود، **والواجهة مفقودة**. أُضيف `components/parent/SpecialistAssessments.tsx` في صفحة التقارير (المقياس، التاريخ، اسم الأخصائي، الشدّة، المجالات، التوصيات، الخطة، إخلاء المسؤولية)، ويُميّز «أجراه الأخصائي» عن «أجبتَ عنه بنفسك» لأن النوعين في مخزن واحد — التمييز عبر `assessedByActorId` الذي يُضبط في الخادم ولا يضبطه مسار الولي. ويظهر القسم **حتى فارغاً** كي لا يتكرر الالتباس نفسه. **ثلاثة عيوب أمسكها التشغيل الحي:** اسم المقياس كان يُطبع `vanderbilt-adhd` خاماً، و`inattention`/`oppositional` بالإنجليزية (نقص في `domainLabels` — أُصلح للغات الثلاث فاستفادت شاشة الأخصائي أيضاً)، و**عيب في الكود الجديد نفسه**: القيمة الابتدائية لـ`useState` تُحسب قبل وصول البيانات فلم يكن أول تقييم يُفتح أبداً |
| الرابعة والثلاثون | **تاريخ ميلاد لا يمكن إدخاله من أي مكان + حجب الملاحظات السريرية:** بلاغ المستخدم كان «كتابتي لتاريخ الميلاد لا تحسب العمر»، والحقيقة أسوأ: الحقل **`disabled` لكل طفل مرتبط**، فالمتصفح يرفض الكتابة أصلاً (أثبته `fill()` بانتهاء المهلة). وعلى طفل **غير** مرتبط يعمل الحساب سليماً، ولهذا بدا السلوك متقطّعاً. و`PATCH /api/admin/students/[id]` كان **يرفض `birthDate`** صراحةً بحجة «هوية لا نتيجة» — فطفل سجلّه بلا تاريخ ميلاد **لا مكان في المنصة كلها لإدخاله**، والعمر يحجب مقاييس فعلاً (صعوبات التعلّم تحت 8، وعتبة DSM-5 تنزل عند 17). **التمييز المعتمد:** *تغيير* تاريخ موجود يبقى مرفوضاً، أما *ملء* تاريخ غائب فليس تغييراً بل إكمال سجل — فصار مسموحاً، مع إعادة حساب `ageGroup` وتسجيله في سجل المراجعة، وإظهار حالة الحفظ بدل ترك قيمة تبدو محفوظة وليست كذلك. وبطلب المستخدم: **ملاحظات الأخصائي السريرية لم تعد تُرسَل للولي** — تُحذف في الخادم لا في الواجهة، لأن طباعة الوثيقة وتسليمها قرار يتخذه الأخصائي في كل مرة، ونشر النص في بوابة تُفتح متى شاءت العائلة ليس الفعل نفسه. 4 فحوص حماية: استبدال تاريخ موجود، `2025-02-30`، تاريخ مستقبلي، وطلب بلا مصادقة — كلها مرفوضة |
| الخامسة والثلاثون | **`ltr-num` كان يقلب 16 نصاً عربياً + شريط خطوات جامد يحبس المستخدم:** (1) بلاغ «التاريخ مقلوب». السبب أن `.ltr-num` يضع `direction: ltr` فينقل الرقم المتصدّر إلى **آخر** السطر. قِستُ مواضع الحروف في متصفح حقيقي (لا تخميناً): «16 سبتمبر 2026 · الأستاذ أمين» ← «سبتمبر 2026 · الأستاذ أمين 16»، «5 مرة» ← «مرة 5»، «250 ر.ق» ← «ر.ق 250». **كل** حالة رقمٌ يسبق عربية كانت مقلوبة. أُزيل من **16 موضعاً**: بطاقة تقييم الولي، منتقي تمارين الجلسة، إشعار المرحلة، بنّاء البرامج، **مبلغ الدفع**، **أسعار الحصة في الإعدادات**، بطاقة الصفحة الرئيسية، شريط المراحل، نافذة إعداد التمرين، وتنشيط المزاج — وبقي حيث هو صحيح (محتوى رقمي خالص يكسره ترتيب RTL). `tests/ltr-num.test.ts` يحرسه ويختبر كاشفه نفسه. (2) **شريط خطوات الحقيبة كان زخرفة جامدة** — `<span>` بلا `onClick`. وحين تصل إلى التقرير، الزر الوحيد الذي يغيّر شيئاً هو «تقييم جديد» **الذي يمحو التقييم كلّه**: من لاحظ اسماً خاطئاً كان عليه تدمير العمل وإعادته. صارت أزراراً مُفعَّلة حيث العودة آمنة — والخطوة 3 تبقى مُعطَّلة عمداً لأن مؤشّر المقياس يكون قد تجاوز نهاية القائمة. **وفخّ فتحه هذا التغيير وأُغلق:** العودة للخطوة 2 وضغط «بدء التقييم» يستدعي `startRun` التي تمسح كل النتائج بلا تحذير — كانت غير قابلة للوصول وصارت على بُعد نقرة، فأُضيف تأكيد |
| السادسة والثلاثون | **البيانات السريرية كانت تُحذف بعد سنة + مقارنة التقييمات:** (1) **الأخطر:** `assessment:` و`apa-record:` و`ld-profile:` كلها تُكتب بـ`EX 31536000` — **٣٦٥ يوماً بالضبط** — بينما قوائم الفهرس التي تشير إليها بلا انتهاء. طفل يُتابَع أكثر من سنة يفقد أقدم سجلاته **بصمت**: المعرّفات تبقى في القائمة والسجلات خلفها تُرجع `null`، فيبدو التاريخ أقصر مما هو. والحقيبة تقول للأخصائي «حُفظ **بشكل دائم**». أُثبت حياً: الحفظ يضبط `31536000`. أُزيلت المهلة من الخمسة مواضع (**اثنان منها في مسار الولي أمسكهما اختبار الانجراف بعد أن فاتا المسح اليدوي**)، وأُضيف `POST /api/admin/maintenance/persist-clinical` (للمالك فقط) يمشي على الفهارس ويُنفّذ `PERSIST` — أُثبت: ٤ مفاتيح نُزعت مهلتها وصفر متبقٍّ. (2) **اسم الأخصائي كان يُحفَظ فارغاً دائماً**: الحفظ يقع لحظة فتح التقرير، والاسم يُكتب بعدها بلحظة، والتأثير يُعيد التشغيل لكن لا يجد شيئاً غير محفوظ فيعود — فالاسم يصل الـPDF المطبوع ولا يصل السجل أبداً، وهو **نفس السطر الذي تراه العائلة الآن في بوابتها**. أُضيف `PATCH /api/assessments` (الاسم والملاحظات فقط — الدرجات هي القياس)، ومعرّف الخادم صار يُحفَظ من رد الـPOST بعد أن كان يُهمَل، والاسم يُحفَظ بين التقييمات. (3) **`redis.set` يُسلسل داخلياً** فثلاثة مواضع كانت تُمرّر نصاً مُسلسَلاً مسبقاً (ترميز مزدوج ينجو بالصدفة لأن `parseEntry` يُخمّن). (4) `VALID_TYPES` مكتوبة يدوياً وقد انحرفت في الاتجاهين: تنقصها `vanderbilt-adhd` وتقبل `anxiety`/`behavior` وهما ليسا نوعي تقييم — رُبطت بالـunion. **وما كان ناقصاً في الحقيبة:** `lib/assessment-compare.ts` — مقارنة المقياس بآخر مرة أُجري فيها على نفس الطفل. **الاتجاه محسوم باختبار يقرأ مكوّنات المقاييس الخمسة** ويتأكد أن كلاً منها يحسب نسبة من **حدّ الأعراض** فالانخفاض تحسّن — مقياس مستقبلي يقيس القدرة بدل العَرَض كان سيقلب كل قراءة بصمت. **ويرفض أن يقول أكثر مما يعلم:** تحرّك دون ٥ نقاط = «ضمن تذبذب التقدير» (بند واحد من ٠-٣ يساوي ٣-٧ نقاط أصلاً)، وفاصل أقل من أسبوعين يُوسَم «قصير»، والمجال **المرتفع يُعرض أولاً** لأنه ما لا يجوز أن يفوت الأخصائي، والنص يقول صراحةً «فارق مُسجَّل بين تقديرين، وليس أثراً علاجياً مقيساً». ١٨ اختباراً للمقارنة + ٥ حراسة انجراف. تحقّق حي: ٦٠ يوماً، الاندفاعية ٤٠←٦٢ أولاً، الانتباه ٧٠←٤٥، وفرط الحركة (+٢) مُستبعَد كضجيج |
| السابعة والثلاثون | **نوافذ قراءة مقطوعة + فحص ملكية مبنيّ على قائمة مبتورة (تدقيق «هل أصلحتَ كل ما ذكرت؟»):** المراجعة كشفت أنني وجدتُ ثلاث مهل انتهاء ولم أُبلّغ بها، ووجدتُ صنفاً كاملاً ولم أُصلحه. **الصنف:** قوائم السجلات تُقرأ بنافذة `lrange(0, 20)`. `getStudentReports` تُغذّي **صفحة تقارير العائلة** و**تصدير البيانات الإداري** — فتقارير طفل تُتابَع لسنتين تختفي من بوابة أهله ومن التصدير معاً، والسجلات نفسها موجودة دائمة. و`assessments:student:` تُقرأ بنفس النافذة: خمسة مقاييس في الحصة تعني أن **الحصة الأولى تختفي بعد أربع حصص** — وهي أيضاً ما تقرؤه مقارنة المقاييس التي أُضيفت للتو. **والأخطر:** `/api/appointments/[id]` كان يشتقّ **الملكية** من `getParentAppointments` المبتورة، فوليّ أمر له أكثر من ٢١ موعداً **يُرفَض وصوله إلى موعده هو**. أُثبت بالتشغيل: ٣٠ موعداً، إلغاء الأقدم ← `403 غير مصرح` قبل الإصلاح و`200` بعده. الملكية صارت تُقرأ من `appointment.parentId` مباشرة — خاصية السجل لا خاصية العرض. وأُضيف حارس انجراف يمنع عودة النافذة، **مع استثناء واحد موسوم صراحةً** (`prompt-window`) لقراءة مولّد البرنامج بالـAI التي تأخذ أحدث ستة تقييمات عمداً لأنها سياق نموذج لا تاريخ طفل. **وما لم يُغيَّر وأُبلغ به صراحةً:** `game-result:` (٣٦٥ يوماً)، `message:` (٩٠ يوماً)، `payment:` (٣٠ يوماً للمعلَّق فقط — المؤكَّد يصير دائماً عند التحديث). الثلاثة قرارات احتفاظ بالبيانات لا أخطاء، وهي قرار صاحب المنصة لا قراري |
| الثامنة والثلاثون | **المراسلات صارت دائمة (بطلبك) + صندوق الوارد كان يُخفي عائلات:** (1) لم تكن الرسائل تنتهي فرادى — **قائمة المحادثة نفسها تحمل `EXPIRE` يُجدَّد مع كل رسالة**، فالمحادثة **كلها** تختفي بعد ٩٠ يوماً من آخر رسالة، ومعها عدّادا «غير المقروء». أُزيلت المهل الأربع، و`getThreadMessages` كانت تقرأ أحدث ٥٠ فقط — فجعل الرسائل دائمة بلا رفع النافذة لا يغيّر شيئاً؛ صارت تقرأ الكل (أُثبت: ٦٣ من ٦٣ بعد أن كانت ٥٠). ووُسِّع مسار الصيانة ليشمل المحادثات (أُثبت: ٣ مفاتيح نُزعت مهلتها، ٦٤ دائمة أصلاً، صفر متبقٍّ). (2) **وعيب أخطر ظهر أثناء الفحص:** `getAdminThreads` تقرأ `lrange('messages:threads:index', 0, 99)` — **صندوق وارد الأخصائي**. والقائمة مرتّبة بـ**تاريخ إنشاء المحادثة** لا بآخر نشاط، فعائلة قديمة تكتب لك اليوم **لا تظهر إطلاقاً** متى وُجدت ١٠٠ محادثة أحدث منها — برسائلها غير المقروءة وكل شيء. وكانت المهلة القديمة تُفاقمها: بعد انتهاء قائمة المحادثة يُحسب الرسالة التالية «أول رسالة» فيُدفع معرّف الولي **مكرّراً** في الفهرس، فتُستهلك خانات من النافذة (التكرار محجوب بـ`Set` عند العرض لا عند القراءة). **أُثبت حياً:** ١٢٠ محادثة أحدث ← قبل الإصلاح ١٠٠ نتيجة والعائلة **غائبة**، بعده ١٢١ و**حاضرة**. (3) وفُحص صنف «الصلاحية مشتقّة من قائمة» في بقية المسارات: ثلاثة فحوص ملكية أخرى تستخدم `getStudentsByParent` وهي تقرأ القائمة كاملة أصلاً — **سليمة**. حارس الانجراف صار يغطي `message:` والفهرسين، ويسمح بـ`lrange(…, 0, 0)` لأنه سؤال وجود لا قراءة تاريخ |
| التاسعة والثلاثون | **البريد كان يبلّغ عن نجاح لم يحدث + التقرير الأسبوعي يتكرّر:** (1) **الأخطر:** مسار Resend في `sendEmail` كان **يتجاهل رد الخادم تماماً** — `await fetch(...)` ثم `return { provider: 'resend' }` بلا فحص. مفتاح مرفوض (401)، أو **نطاق مُرسِل غير موثَّق** (403 — والعنوان `noreply@amine-academy.com` مكتوب في الكود، فإن لم يكن نطاقك الموثَّق في Resend **يفشل كل إرسال**)، أو تجاوز حدّ (429) — كلها تبدو للمستدعي **إرسالاً ناجحاً**. وهذا أسوأ موضع ممكن لفشل صامت: رابط التفعيل الذي ينتظره ولي أمر، وإشعار «التقرير جاهز»، والـcron الأسبوعي الذي **يكتب مفتاح عدم التكرار عند النجاح فلا يُعيد المحاولة تلك الجمعة أبداً**. صار يفحص `res.ok` ويرفع خطأً يحمل شرح Resend نفسه، وأُضيف `RESEND_FROM` لأن النطاق الموثَّق إعداد نشر لا ثابت. ٨ اختبارات — **أربعة منها تفشل على الكود القديم وتنجح على الجديد** (رد الرفض لا يمكن إنتاجه بلا حساب Resend، فالرد مُستبدَل والفرع المُختبَر هو قرار الكود بشأن ما عاد إليه). (2) **`weekly-report` بلا أي حماية من التكرار** بينما `send-reminder` كان يحميها منذ البداية: إعادة محاولة، أو تشغيل يدوي بعد فشل مزوّد البريد في منتصف القائمة، يُرسل نسخة ثانية لكل عائلة وصلتها الأولى. أُضيف `weekly-report:{أسبوع}:{ولي}` **بعد** نجاح الإرسال لا قبله (فالعائلة التي فشل بريدها تُعاد المحاولة لها)، ما يجعل التشغيل الجزئي **قابلاً للاستئناف**. أُثبت حياً: `skipped: 1, errors: 0`. (3) وفُحص `check-subscriptions` ووُجد **آمن التكرار** أصلاً (ضبط `expired` مرتين لا أثر له، ويتخطّى `expiry: null` الخاص بالحسابات الحضورية) — لم يُلمَس |
| الأربعون | **المزامنة اللحظية كانت تموت بصمت والمؤشر يقول «مباشر»:** فُحصت طبقة Pusher لأول مرة. صفحة الجلسة تحمل **عدة اشتراكات على القناة نفسها في آنٍ واحد**: تأثير الجاهزية، وتأثير الجلسة الرئيسي، و`StoryReader` ما دام تمرين القصة مفتوحاً. و`pusher-js` **لا يحتفظ بعدّاد لكل ربط**: `unsubscribe(name)` يُسقط **القناة كلها**. فأيّهم يُغادر أولاً (إغلاق القصة عند نهاية التمرين، أو إعادة تشغيل تأثير) **يقتل اللحظية للبقية** — والمؤشر يظل يقول «مباشر» لأن **المقبس** ما زال متصلاً، والمفقود هو القناة وحدها؛ فتتدهور المنصة إلى التحديث الدوري بلا أي إشارة. أُضيف **عدّاد مرجعي لكل قناة** (لا للاتصال وحده) فلا تُسقَط القناة إلا عند مغادرة آخر مشترك. **وعيبان آخران في الملف نفسه:** (٢) `subscribeSession('')` كان يستدعي `acquire()` **ثم** يعود بمنظّف فارغ — فيتسرّب مرجع اتصال ولا يُغلق المقبس أبداً؛ صار الفحص **قبل** `acquire`. (٣) المنظّفات لم تكن idempotent، فتشغيلها مرتين يُنقص العدّاد مرتين ويُسقط قناة ما زالت مستعملة. **الإثبات:** ٨ اختبارات بمحاكاة تُطابق سلوك `pusher-js` (كائن قناة مشترك لكل اسم، و`unsubscribe` يُزيله، وبثّ لا يصل إلا لقناة حيّة) — **خمسة منها تفشل على الكود القديم وتنجح على الجديد**. **وصنف «استدعاء خارجي بلا فحص رد» فُحص كاملاً:** Telegram يُحذّر، وDaily يرفع خطأً، وreCAPTCHA يفشل **مفتوحاً عمداً وموثّقاً** (مع فخّ العسل وتوقيت الإرسال كبديل) — نظيفة ولم تُلمَس |
| الحادية والأربعون | **النسخ الاحتياطي: كامل، مشفَّر، تلقائي، وقابل للاستعادة:** التصدير القديم كان يُنزّل ملفاً اسمه `amine-academy-backup-<date>.json` **بلا تقييم واحد** — لا فاندربيلت ولا PSC-17 ولا حصص APA ولا رسالة واحدة. كان يحمل `assessmentProfiles` وهو سجل آخر تماماً (تفضيلات تمارين الطفل لا ما قاسه أي مقياس). أي أن الملف الذي ستفتحه **يوم ضياع كل شيء** لا يحوي العمل السريري. `lib/backup.ts` يجمع **١٧ نوع سجل** بعدّاد لكل نوع (فالقسم الفارغ **يُرى** لا يُفترض) وقائمة أخطاء لكل قسم فشلت قراءته — فالنسخة الناقصة تُعلن نقصها بدل أن تبدو كاملة. والتصدير اليدوي والوظيفة الليلية **يستعملان الباني نفسه** فلا ينحرفان ثانية. **التشفير غير اختياري:** Vercel Blob يخدم كل كائن من **رابط عام**، و«رابط لا يُخمَّن» ليس ضابطاً تضع خلفه سجلات أطفال سريرية — فما يُرفَع هو نصّ مشفَّر بـAES-256-GCM (وهو يُوثّق أيضاً: أرشيف مُعدَّل **يفشل فتحه** بدل أن يُفك إلى شيء معقول). و**يفشل مغلقاً**: بلا `BACKUP_ENCRYPTION_KEY` لا رفع إطلاقاً، والرد يقول أي متغيّر ناقص بدل نجاح صامت. `scripts/decrypt-backup.mjs` **ليس ترفاً**: أرشيف لا يُفتح ليس نسخة احتياطية — وهو نفس صنف العيب الذي نطارده. و`GET /api/admin/backups` يعرض الأرشيفات وعمر آخرها ويُحذّر إن تجاوز ٤٨ ساعة أو لم يُفعَّل أصلاً. **تحقّق كامل:** التصدير يحوي التقييم وحصص APA و١٢٢ رسالة، و`passwordHash` **لا يظهر**، والأرشيف المشفَّر لا يحوي «هجرس» نصاً، والاستعادة بالسكربت **مطابقة بايت ببايت**، والمفتاح الخاطئ والأرشيف المُعدَّل **كلاهما يُرفَض**. ١٥ اختباراً منها حارس يقرأ مخطط المفاتيح في `db.ts` فيُفشل البناء إن أُضيف نوع سجل بلا موضع في النسخة |
| الثانية والأربعون | **الملف المعرفي — بديل صادق عن «مقياس الذكاء»:** السؤال كان «هل يمكن إضافة مقياس ذكاء». الجواب: **لا** — WISC-V وStanford-Binet مملوكة تجارياً (تنفيذ بنودها مخالفة)، ومقيَّدة بـ**Level C** (دكتوراه في علم النفس، خارج نطاق أخصائي APA)، وتطبيقها وجهاً لوجه جزء من الأداة، و**المعايير هي المنتَج** فلا توجد نسخة مجانية مُقنَّنة. والأخطر: رقم ذكاء أنتجه غير مؤهّل، في وثيقة **تُقرأ في مدرسة**، يلاحق طفلاً سنوات. **لكن الفحص كشف أن البطارية موجودة أصلاً**: `lib/cognitive-tasks.ts` فيه ٦ مهام أداء حقيقية (مدى أمامي وعكسي، ذاكرة سمعية، CPT، بحث بصري، ستروب) بحدود عمرية وقراءات صادقة — **و`perfResults` لم تكن تُحفَظ أبداً**: تعيش في المسودة وتُطبَع، ثم تتبخّر آخر كل جلسة. لا خط أساس، ولا سجل، ولا نسخة احتياطية، ولا شيء تُقارَن به الجولة القادمة — **نفس صنف العيب** في أثمن ما تُنتجه المنصة (قياس لا تقدير). أُضيف `lib/cognitive-profile.ts` يملأ الخانة الفارغة `type: 'cognitive'`: يقارن الطفل **بنفسه** (أقوى وأضعف مجالاته)، **بلا درجة كلية** (`totalScore: 0` عمداً) و**بلا شدّة** (`severity: 'none'` — هذه مهام أداء لا يُلصَق بها تصنيف) وبلا مئين. **والخطر الأكبر كان الاتجاه:** هذه المهام **قدرة** (الارتفاع تحسّن) عكس كل مقاييس الحقيبة — فقراءتها كأعراض كانت ستُبلّغ عن طفل تحسّن بأنه تدهور. أُضيف `ABILITY_SCALES` إلى `assessment-compare` مع اختبار يُثبت أن **نفس الرقمين يُقرآن عكسياً** على المقياسين، وصياغة «تحسّن/تراجع» بدل «انخفض/ارتفع». وحمايات: أقل من ٣ مهام قابلة للتفسير = «لا يكفي لملف»، وفارق دون ١٢ نقطة = «أداء متقارب» لا قوة مصطنعة، ومهمة محجوبة بالعمر (ستروب تحت ٨) **تُستبعَد** فلا تصير «ضعف الطفل»، والانتباه المستمر لا يُسجَّل بمعدّل الإصابة وحده بل بالإنذارات الكاذبة معه. **وعيب أمسكه التشغيل:** بوابة التقرير كانت `results.length === 0` فجلسة قاست الأداء بلا مقياس تقدير **تُخفي الأرقام المقاسة كلها**. ٢١ اختباراً. تحقّق حي: البطارية تُحفَظ فعلاً، و٦٠ يوماً، و«الانتباه المستمر 80 ← 87 **تحسّن**» و«الانتقائي 62 ← 40 **تراجع**» معروضاً أولاً |
| الثالثة والأربعون | **الدخول بحساب Google لأولياء الأمور + تصحيح اسم الطفل:** (1) بلاغ المستخدم: «لم أستطع تغيير اسم الطفل لأنني أخطأت به». `PATCH /api/admin/students/[id]` كان يرفض الاسم بحجّة **«هوية لا نتيجة»** — قاعدة كتبتُها أنا، وهي خاطئة عملياً: خطأ إملائي لحظة الإنشاء يصير **دائماً**، والاسم مطبوع في الوثيقة التي تُسلَّم للعائلة وتُعرض في المدرسة. الرفض لا يحمي أحداً: الوحيد الذي يصل هذا المسار هو الأخصائي الذي كتب الاسم أصلاً. صار قابلاً للتصحيح، **عبر `lib/person-name.ts`** نفسه (فالتصحيح ليس منفذاً للالتفاف على التعقيم)، و**سجل المراجعة يحفظ الإملاء السابق**. تاريخ الميلاد يحتفظ بقاعدته: ملء الغائب مسموح، تغيير المسجَّل مرفوض. (2) **الدخول بحساب Google** — مبنيّ يدوياً لا بـNextAuth: المشروع يُصادق بجلسات HMAC خاصة به، وإضافة منظومة جلسات ثانية على منصة تحمل سجلات أطفال تعني **مصدرَي حقيقة** عن هوية الداخل. تدفّق code + **PKCE (S256)** + `state` في كوكي httpOnly، والنطاق `openid email profile` فقط بلا وصول دائم. **والقرار المحوري: لا يُنشئ حساباً أبداً.** Google تُثبت *من* أنت، ولا تقول إن طفلاً قد قُبِل ولا ما قاله الاستقبال — فالبريد بلا حساب يُحوَّل إلى **نموذج التسجيل العادي** مُعبّأً بالاسم والبريد، فلا تنبت حسابات أولياء فارغة في سجلاتك. **و`email_verified` هو الحارس الأهم**: Google تُصدر عناوين غير موثَّقة لبعض أنواع الحسابات، واعتبارها إثباتاً يعني أن من يدّعي بريد ولي أمر يدخل إلى ملفات أطفاله. ويُطبَّق نفس ما يطبّقه دخول كلمة المرور (الحساب الموقوف يبقى موقوفاً)، **وحساب ينتظر رمز تفعيل يُعتبر موثَّقاً** لأن الرمز وُجد ليُثبت ما أثبتته Google للتو. **المعرّف العام والسرّ منفصلان**: `NEXT_PUBLIC_GOOGLE_CLIENT_ID` ليس سرّاً (يظهر في كل رابط تفويض) فتقرّر الصفحة رسم الزر بلا طلب إضافي، و`GOOGLE_CLIENT_SECRET` في الخادم وحده؛ وبلا الاثنين لا زر ولا مسار (503) — نفس تدهور Pusher وreCAPTCHA. ٢٢ اختباراً تغطّي: رمز مُصدَر لتطبيق آخر، ومنتهي الصلاحية، ومُصدِر ليس Google، و**بريد غير موثَّق يُرفض حتى مع وجود حساب مطابق**. **٤ فحوص حيّة للتزوير:** بلا `state`، و`state` غير مطابق، وإلغاء، ورمز مزوّر — **ولم تصدر جلسة في أيٍّ منها** |
