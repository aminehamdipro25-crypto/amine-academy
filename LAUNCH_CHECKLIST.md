# 🚀 قائمة تحقّق الإطلاق — أكاديمية أمين

> مرجع عملي لما يجب التأكّد منه قبل فتح المنصة لعائلات حقيقية.
> رتّبها الخبير بحسب الأولوية. علّم ✅ عند إنجاز كل بند.

---

## 🔴 حرج — لا تُطلق قبل إنجازها

### إعداد المتغيّرات في Vercel (Settings → Environment Variables)
| المتغيّر | إلزامي | ماذا يحدث إن غاب |
|---|---|---|
| `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` | ✅ نعم | لا شيء يعمل (قاعدة البيانات) |
| `AUTH_SECRET` (٦٤ حرفاً — `openssl rand -hex 32`) | ✅ نعم | التطبيق **لا يُقلع** (يرمي خطأ) |
| `ADMIN_PASSWORD` (قوية) | ✅ نعم | لا يمكن دخول لوحة الأخصائي |
| `DAILY_API_KEY` | ⚠️ **حرج للفيديو** | **الجلسة المباشرة بالفيديو — قلب المنصة — لا تعمل** |
| `RESEND_API_KEY` أو `GMAIL_USER`+`GMAIL_APP_PASSWORD` | مهم | لا إيميلات تفعيل/تذكير |
| `CRON_SECRET` | مهم | تذكيرات المواعيد + فحص الاشتراكات لا تعمل |
| `NEXT_PUBLIC_BASE_URL` | مهم | روابط الإيميلات/SEO خاطئة |

**بعد إضافة أي متغيّر جديد: أعد النشر (Deployments → ⋮ → Redeploy)** — المتغيّرات لا تُطبَّق على النشر الحالي تلقائياً.

### اختبار دخان (Smoke test) قبل الإطلاق
- [ ] مكالمة فيديو حقيقية تعمل من طرفين (أخصائي + طفل على جهازين).
- [ ] تسجيل ولي أمر جديد → وصول إيميل التفعيل → التفعيل → الدخول للبوابة.
- [ ] حجز جلسة تقييمية مجانية → ظهورها في لوحتك بشارة «🎁 تقييم مجاني».
- [ ] جلسة كاملة: بدء تمرين → مزامنته على شاشة الطفل → حفظ التقرير.
- [ ] مكتبة القصص: تعديل قصة + تلوين كلمة + إضافة صورة → ظهورها في بوابة الوالد.

---

## ⚠️ مهم — أنجزها حول وقت الإطلاق

### أمان وحسابات
- [ ] غيّر `ADMIN_PASSWORD` عن أي قيمة افتراضية سابقة.
- [ ] (موصى) reCAPTCHA v3: أضِف `NEXT_PUBLIC_RECAPTCHA_SITE_KEY` + `RECAPTCHA_SECRET_KEY` (الفخّ + التوقيت يعملان بدونها).
- [ ] فعّل **النسخ الاحتياطي في Upstash** (الخطة المدفوعة) — بياناتك كلها في Redis.

### تشغيل
- [ ] (موصى) **Sentry**: راقبة أخطاء خارجية فورية. المراقبة الداخلية (`/dashboard/monitor`) موجودة لكنها لا تعمل إن تعطّل التطبيق كلياً.
- [ ] خطّة طوارئ: ماذا لو مرضت/انقطع اتصالك أثناء جلسة مجدولة.
- [ ] راقب لوحة `/dashboard/monitor` أول أسبوع بعد الإطلاق.

### قانوني (لا يكفيه الكود — يحتاج مختصاً بشرياً)
- [ ] **مراجعة قانونية** لادّعاء «COPPA & GDPR Compliant» — تجمع بيانات صحية لأطفال + فيديو لقُصّر.
- [ ] وثّق في سياسة الخصوصية: هل الفيديو يُسجَّل؟ (حالياً: لا — وثّق ذلك صراحةً).
- [ ] راجع اتفاقيات معالجة البيانات (DPA) مع المزوّدين: Upstash · Daily.co · Resend · Vercel.

### سريري
- [ ] (للتوسّع) مراجعة سريرية بشرية لمحتوى القصص/التمارين التي تؤلّفها.
- [ ] تأكّد أن إخلاء المسؤولية الطبي ظاهر (موجود في صفحة التقييم والخصوصية).

---

## 🟢 اختياري — تحسينات لاحقة (لا تمنع الإطلاق)

- [ ] `PUSHER_*` → مزامنة لحظية ~١٠٠ms بدل استطلاع ~١ث (تعمل بدونها).
- [ ] `BLOB_READ_WRITE_TOKEN` → رفع الصور مباشرة (لصق الرابط يكفي بدونها).
- [ ] تحسين أداء قراءات القوائم إن تجاوزت مئات العائلات (تم بالفعل تجميع القراءات دفعياً `mget`).
- [ ] `next/image` لصور القصص عند نمو المكتبة.
- [ ] اختبار حِمل قبل التسويق الواسع.

---

## 📌 المعمار — للرجوع السريع
- **قاعدة البيانات:** Upstash Redis (كل شيء). المصادقة HMAC تصمد لو تعطّل Redis مؤقتاً.
- **الفيديو:** Daily.co (prebuilt iframe). **المزامنة:** Pusher (إيقاظ) + Redis (مصدر الحقيقة) مع استطلاع احتياطي.
- **CI:** `.github/workflows/ci.yml` يفحص TypeScript + الاختبارات على كل push و PR. **فعّل حماية الفرع `main`** لتطلب نجاح فحص «test».
- **الأمان:** كل مسارات التعديل محمية (مالك/طاقم/ولي)، لا XSS، روابط الصور https فقط، حدّ معدّل على العام.

_آخر تحديث: مراجعة جاهزية الإطلاق الشاملة._
